{
  "name": "traced-worker",
  "version": "1.0.0",
  "description": "Traced email tracking — Cloudflare Worker",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
